services:
  resume:
    depends_on:
      - nginx
    restart: unless-stopped
    ports:
      - "8080:8080"
    image: alexeynaidiuk/resume:latest
    build:
    command: 'uv run --no-dev python -m app.main'
    container_name: resume_automation
